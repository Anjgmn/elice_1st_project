const baseUrl = 'http://kdt-ai6-team05.elicecoding.com/api/v1/categories';

const requestUrls = {
  GET_ALL_CATEGORIES: `${baseUrl}`,
  CREATE_CATEGORY: `${baseUrl}`,
  UPDATE_CATEGORY: `${baseUrl}`,
  DELETE_CATEGORY: `${baseUrl}`,
};

export default requestUrls;